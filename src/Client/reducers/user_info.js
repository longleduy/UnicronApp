
const _initialState = {
    isAuthen: false
}
const user_info = (state = _initialState, action) => {
    switch (action.type) {
        default:
            return { ...state };
    }
}
export default user_info;